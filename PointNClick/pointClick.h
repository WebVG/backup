#include istream
#include


namespace name

void main ()

{
	// what loads first in a game? variables //
	char[4] forrest, desert, iceland, ocean, space
		forrest = (C:\\picture), etc // how many times does a referenced variable get referenced.
	
	// console should ask for login for game to start //
	displaywindow(userinput)
		x=userinput
		
	class = new class login
	
		private usrnme, passwrd {
			// login should store the information
		}
		
				
		class = new class background{
			cout(C:\\picture.bmp);	// picture is loaded
			switch {
				case 0 << (C:\\forrest.bmp);
				case 1 << (C:\\desert.bmp);
				case 2 << (C:\\iceland.bmp);
				case 3 << (C:\\ocean.bmp);
				case 4 << (C:\\space.bmp);
			}
		}
		
		class = new class controls
		
			// consider making walking, running, etc methods of the controls class. //
			// code for walking, it should toggle, this requires discrete recursion //
			
			while true (button.press = 1){
				forinti=0i<xi++
			}
		// map and graphing // 

		// controls //
		
		// how will the computer process clicks //
		
		// how will the screen load //
	
	
	return 0;
	/*
	keep this simple and make it expandable, think rayman.
	*/
}